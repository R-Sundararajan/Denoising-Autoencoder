You are a senior Python ML engineer and full-stack Streamlit developer.

Build a complete, beginner-friendly project for image/PDF denoising and reconstruction using deep learning.

Project goal:
- The app must accept an image or PDF as input.
- The user can optionally add synthetic noise using a slider.
- The noisy input is sent to a selected model.
- The output is shown in a clear before/after comparison.
- The result can be downloaded as an image or PDF.
- The system must support switching between models:
  1. Simple Autoencoder
  2. U-Net
- The code should be modular, simple, and easy to expand.
- Use Python, PyTorch, Streamlit, PIL/OpenCV, NumPy.
- For PDFs, process page by page and return either separate denoised pages or a combined output PDF.
- The models will later be trained on large document datasets using synthetic noise such as Gaussian noise, blur, and salt-and-pepper noise. Keep the training pipeline ready for that.

Important design requirements:
- Keep the UI very simple and intuitive.
- Show original image, noisy image, and restored image side by side.
- Add a slider for noise strength.
- Add a model selector dropdown.
- Add a download button for the restored output.
- Include support for both images and PDFs.
- Add basic preprocessing and postprocessing utilities.
- Provide a training script and an inference script.
- Keep the folder structure simple and clean.
- Do not over-engineer. Prefer readable code over advanced abstractions.

Please generate:
1. A simple folder structure
2. Full code for each important file
3. A `requirements.txt`
4. A short `README.md` with setup and run instructions
5. Notes on where to place trained weights
6. A minimal training pipeline for synthetic noise generation

Use this folder structure:

doc_denoiser/
├── app.py
├── requirements.txt
├── README.md
├── data/
│   ├── raw/
│   └── processed/
├── models/
│   ├── __init__.py
│   ├── autoencoder.py
│   ├── unet.py
│   └── weights/
├── training/
│   ├── __init__.py
│   ├── train.py
│   ├── dataset.py
│   └── augment.py
├── inference/
│   ├── __init__.py
│   ├── predict.py
│   ├── image_utils.py
│   └── pdf_utils.py
├── utils/
│   ├── __init__.py
│   ├── io_utils.py
│   └── visualization.py
└── outputs/
    ├── images/
    └── pdfs/

Functional expectations:
- `app.py` should be the Streamlit UI.
- `training/train.py` should support training either model.
- `training/augment.py` should generate synthetic noise.
- `inference/predict.py` should load model weights and run inference.
- `inference/pdf_utils.py` should extract pages from PDFs and rebuild output PDFs.
- `utils/visualization.py` should handle comparison display.
- Add clean comments explaining each section.
- Keep code compatible with a normal Windows Python environment.

Model guidance:
- Autoencoder should be a basic convolutional encoder-decoder.
- U-Net should be a stronger restoration model.
- Make the model selection easy in the UI.
- If weights are missing, the app should show a clear message instead of crashing.

Output format:
- First show the folder structure.
- Then provide the code file by file.
- Then explain how to run training and how to launch the Streamlit app.
- If any part is too long, split it into logical chunks but keep everything complete.

Also include:
- How synthetic noise is applied during training
- How inference differs for images vs PDFs
- How to save the final restored result
- A simple recommendation for which model to use first

Keep the response practical and ready to copy into a real project.